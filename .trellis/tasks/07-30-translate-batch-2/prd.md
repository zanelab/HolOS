# Batch 2 Markdown localization

## Requirements
- Translate all remaining English prose in the files listed by `/opt/data/holos-translate-work/batch-2.txt` to natural Simplified Chinese.
- Process files sequentially and edit them in place.
- Preserve fenced code blocks byte-for-byte, plus inline code, identifiers, package names, paths, URLs, keys, commands, versions, and API names.
- Translate headings, quotes, list prose, HTML comments, and table headers/cells.

## Acceptance criteria
- Every listed file is processed.
- No new files are created and no listed content is deleted.
- Snapshot verification confirms all fenced code blocks match the originals.
