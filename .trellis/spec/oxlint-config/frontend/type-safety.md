# @vben/oxlint-config Type Safety

> Strict-mode TS.

```ts
import type { Linter } from 'eslint';
export const config: Linter.Config[] = [...];
```

## Forbidden

- Don't disable strict mode
- Don't use any
